# Environment variables for Cloud Functions (project: calorie-app-chat).
# GEMINI_API_KEY is provided via Secret Manager (firebase functions:secrets:set GEMINI_API_KEY).
# Do not add GEMINI_API_KEY here — it conflicts with the secret.
