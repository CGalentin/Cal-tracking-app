{
  "functions": {
    "source": "functions",
    "predeploy": "cd functions && npm install && npm run build"
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "storage": {
    "rules": "storage.rules"
  }
}
